{
  "name": "poppin-match",
  "version": "1.0.0",
  "private": false,
  "description": "The recognition engine behind Poppin: deciding when a page is about a tradeable asset, and when to stay quiet.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
